import 'package:flutter/material.dart';
import 'package:flutter_application_2/presentacion/widgets/widgets.dart';

class TomarFotografiaScreen extends StatelessWidget {
  static const nombre = 'tomar_fotografia';
  const TomarFotografiaScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      appBar: AppBarLogoOscusWidget(),
      body: Column(),
    );
  }
}
